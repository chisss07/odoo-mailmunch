# REQUIRED: Generate a Fernet key with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# REQUIRED: Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

DATABASE_URL=postgresql+asyncpg://mailmunch:mailmunch@postgres:5432/mailmunch
REDIS_URL=redis://redis:6379

# Optional: M365 (configure via Settings UI after first login)
# M365_TENANT_ID=
# M365_CLIENT_ID=
# M365_CLIENT_SECRET=
